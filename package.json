{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present run build && mkdir -p lib/dist && cp -r lib/dist/. ./lib/dist/ 2>/dev/null || true",
    "start": "node lib/dist/index.js"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}


